apiVersion: v1
kind: Service
metadata:
  name: {{ include "main-node.fullname" . }}
  labels:
{{ include "main-node.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "main-node.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
