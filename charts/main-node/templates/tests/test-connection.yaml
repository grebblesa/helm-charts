apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "main-node.fullname" . }}-test-connection"
  labels:
{{ include "main-node.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "main-node.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
